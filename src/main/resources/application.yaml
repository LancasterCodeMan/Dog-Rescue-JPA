spring:
   datasource:
      username: dog_rescue
      password: dog_rescue
      url: jdbc:mysql://DC1:3306/dog_rescue

   jpa:
      hibernate:
         ddl-auto: none
      show-sql: true

   sql:
      init:
         mode: always